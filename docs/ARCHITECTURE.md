# 🏗️ Arquitectura de CrypticOnline

## Diagrama general

```
┌─────────────────────────────────────────────────────────────┐
│                    NAVEGADOR DEL USUARIO                     │
└────────────────────┬────────────────────────────────────────┘
                     │ HTTP/HTTPS
                     ▼
┌──────────────────────────────────────────────────────────────┐
│                      FRONTEND (React/Vue)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────────┐   │
│  │  Auth Pages  │  │Challenge Lib │  │ Admin Dashboard │   │
│  └──────────────┘  └──────────────┘  └─────────────────┘   │
│         │                │                     │             │
│         └────────────────┴─────────────────────┘             │
│                    Services Layer                           │
└────────────────────┬────────────────────────────────────────┘
                     │ REST API (JSON)
                     ▼
┌──────────────────────────────────────────────────────────────┐
│                    BACKEND (Express.js)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐  │
│  │ Auth API │  │Challenge │  │ Users    │  │Admin API   │  │
│  └──────────┘  └──────────┘  └──────────┘  └────────────┘  │
│         │            │            │              │           │
│         └────────────┴────────────┴──────────────┘           │
│              Controllers & Business Logic                    │
│                      │                                       │
│                Middleware (Auth, CORS, etc)                  │
└────────────────────┬────────────────────────────────────────┘
                     │ SQL/NoSQL Query
                     ▼
┌──────────────────────────────────────────────────────────────┐
│                      BASE DE DATOS                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐  │
│  │  Users   │  │Challenges│  │ Progress │  │ Scoreboard │  │
│  └──────────┘  └──────────┘  └──────────┘  └────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

## Capas de la aplicación

### 1. **Capa de Presentación (Frontend)**
- **Tecnología**: React/Vue.js
- **Responsabilidades**:
  - Interfaz de usuario responsiva
  - Formularios de autenticación
  - Visualización de desafíos
  - Dashboard de progreso

### 2. **Capa de API (Backend)**
- **Tecnología**: Express.js
- **Responsabilidades**:
  - Procesamiento de solicitudes HTTP
  - Validación de datos
  - Autenticación y autorización
  - Lógica de desafíos

### 3. **Capa de Datos**
- **Tecnología**: MongoDB/PostgreSQL
- **Responsabilidades**:
  - Almacenamiento persistente
  - Integridad referencial
  - Índices y optimización

## Flujo de datos típico

```
1. Usuario → Realiza acción en Frontend
2. Frontend → Envía petición HTTP a API
3. Backend → Valida y procesa petición
4. Backend → Consulta/modifica Base de Datos
5. Base de Datos → Retorna datos
6. Backend → Envía respuesta JSON
7. Frontend → Actualiza interfaz
```

## Stack tecnológico

| Componente | Tecnología |
|-----------|-----------|
| Frontend | React/Vue.js, Axios/Fetch |
| Backend | Node.js, Express.js |
| Base de Datos | MongoDB/PostgreSQL |
| Autenticación | JWT (JSON Web Tokens) |
| Seguridad | bcryptjs, CORS |
| Testing | Jest, Supertest |

## Principios de diseño

- **Separación de responsabilidades**: Cada capa tiene roles específicos
- **DRY (Don't Repeat Yourself)**: Reutilización de código
- **MVC Pattern**: Model-View-Controller en backend
- **RESTful API**: Estándares de API web
- **Seguridad**: Validación, sanitización y encriptación
